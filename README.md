Repository to demonstrate how to take medical images and convert them into virtual reality object files.
The bash script takes in DICOMDIR, converts the DICOM to a NIfTI file, segments the image with TotalSegmentator, then converts the segmentations to 3D object files.
These object files can be uploaded to a variety of 3D object viewers.

Citable paper coming soon!
